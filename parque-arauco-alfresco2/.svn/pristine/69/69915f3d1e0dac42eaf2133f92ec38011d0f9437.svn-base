  <#if childrenStatus!="-1">
	<ul class="classificationUl">
	<#list childrens as node> 	
		<li class="element folder parent" id="${node.uuid}" >
			<img src="/share/images/icons/close_folder.png" class="element" onClick="toggle('${node.uuid}')" />
			<label id="label_${node.uuid}" class="elementLabel element" onClick="selectNode('${node.uuid}')">${node.name} (calculando...)</label>
		</li>
	</#list>
	<ul>
  </#if>
